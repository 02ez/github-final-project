name: Enterprise CI/CD
on: [push, pull_request]

jobs:
  redirect-to-enterprise-workflows:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "🚀 Enterprise CI/CD workflows are now active!"
          echo "📋 Build and Test: .github/workflows/build-test.yaml"
          echo "🔒 Security Scan: .github/workflows/security-scan.yaml"
          echo "📦 Supply Chain: .github/workflows/supply-chain.yaml"
          echo "🚀 Deploy: .github/workflows/deploy.yaml"
          echo "✅ This workflow is maintained for backward compatibility"